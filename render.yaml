services:
  - type: web
    name: gold-scraper
    env: node
    plan: free
    region: singapore

    buildCommand: npm install
    startCommand: node server.js

    envVars:
      - key: NODE_VERSION
        value: 18

      - key: NODE_ENV
        value: production
